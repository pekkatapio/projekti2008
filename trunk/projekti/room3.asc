// room script file

function hHotspot1_Interact()
{
  
  player.ChangeRoom(1, 57, 252);
}

function hHotspot2_Interact()
{
  player.Walk(42, 196, eBlock);
 player.ChangeRoom(4, 190, 240);
}

function hHotspot3_AnyClick()
{
  player.Walk(563, 140, eBlock);
 Display("The Door is locked!");
}

function room_LeaveLeft()
{
player.ChangeRoom(7, 453, 119);
}

function hHotspot5_AnyClick()
{
  player.Walk(293, 139, eBlock);
  player.ChangeRoom(16, 57, 252);
}

function hHotspot4_AnyClick()
{
  player.Walk(421, 139, eBlock);
 player.ChangeRoom(17, 57, 252);
}

function region1_WalksOnto()
{
 player.ChangeRoom(7, 443, 84);
}

function room_FirstLoad()
{
  cPaski.Say("Mauripekka. Eikˆ sinun pit‰isi olla tunneilla?");
  cEgo.Say("Olen kokoajan menossa.");
  
}

function room_RepExec()
{
  // Tutkitaan, onko ajastin k‰ynyt.
  if (IsTimerExpired(1)) {
     
     // Arvotaan uusi y-koordinaatti
     int uusiy = Random(50)+150;
     int uusix = 0;

     // Tutkitaan kumpaan p‰‰t‰ k‰yt‰v‰‰ seuraavaksi k‰vell‰‰n..
     if (pask_paikka == 0) { 
       uusix = Random(100)+500;
       pask_paikka = 1;
     } else {
       uusix = Random(150)+50;
       pask_paikka = 0;
     } 
     
     // K‰vell‰‰n uuteen pisteeseen.
     cPaski.Walk(uusix, uusiy, eNoBlock);
     
     // Asetetaan ajastin.
     SetTimer(1,Random(300)+500); 
  }  
}

function room_AfterFadeIn()
{
  // K‰ynnistet‰‰n ajastin..
  SetTimer(1,200); 
}
